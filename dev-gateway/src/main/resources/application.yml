spring:
  application:
    name: dev-gateway
  profiles:
    active: dev,rpc
---

spring:
  profiles: dev

GATE_BASE: /Users/zhangyong/workspace/allinfinance/frame/dev-latest/dev-gateway/src/main/resources/

gate:
  log-base-path: ${GATE_BASE}/logs/${spring.application.name}/
---

spring:
  profiles: sit

GATE_BASE: ${APP_BASE}/apps/${spring.application.name}/config/

gate:
  log-base-path: ${APP_BASE}/logs/${spring.application.name}