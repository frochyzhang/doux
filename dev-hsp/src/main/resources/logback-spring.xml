<?xml version="1.0" encoding="UTF-8"?>

<configuration>
    <!--
    说明：
        1. 文件的命名和加载顺序有关
           logback.xml早于application.yml加载，logback-spring.xml晚于application.yml加载
           如果logback配置需要使用application.yml中的属性，需要命名为logback-spring.xml
        2. logback使用application.yml中的属性
           使用springProperty才可使用application.yml中的值 可以设置默认值
    -->
    <!-- 通用参数配置 -->
    <springProperty scope="context" name="APP_NAME" source="spring.application.name"/>
    <springProperty scope="context" name="LOG_HOME" source="dev-hsp.log.base-path"
                    defaultValue="${HOME}/logs/${APP_NAME}/"/>
    <springProperty scope="context" name="LOG_LEVEL" source="dev-hsp.log.level" defaultValue="info"/>

    <!--SKY-WALKING日志收集-->
    <appender name="GRPC-LOG" class="org.apache.skywalking.apm.toolkit.log.logback.v1.x.log.GRPCLogClientAppender">
        <encoder class="ch.qos.logback.core.encoder.LayoutWrappingEncoder">
            <layout class="org.apache.skywalking.apm.toolkit.log.logback.v1.x.TraceIdPatternLogbackLayout">
                <Pattern>%date [%contextName] %level [%thread] [%tid] %logger{36} [%file : %line] %msg%n</Pattern>
            </layout>
        </encoder>
    </appender>

    <!-- 文件输出 -->
    <appender name="HSP-ALL" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <!--<Encoding>UTF-8</Encoding>-->
        <file>${LOG_HOME}/${APP_NAME}.log</file>

        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_HOME}/${APP_NAME}.log.%d{yyyy-MM-dd}.%i.zip</fileNamePattern>
            <maxFileSize>200MB</maxFileSize>
        </rollingPolicy>
        <encoder class="ch.qos.logback.core.encoder.LayoutWrappingEncoder">
            <layout class="org.apache.skywalking.apm.toolkit.log.logback.v1.x.TraceIdPatternLogbackLayout">
                <Pattern>%date [%contextName] %level [%thread] [%tid] %logger{36} [%file : %line] %msg%n</Pattern>
            </layout>
        </encoder>
    </appender>

    <!-- 控制台输出 -->
    <appender name="HSP-CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">-->
            <level>${LOG_LEVEL}</level>
        </filter>
        <encoder class="ch.qos.logback.core.encoder.LayoutWrappingEncoder">
            <layout class="org.apache.skywalking.apm.toolkit.log.logback.v1.x.TraceIdPatternLogbackLayout">
                <Pattern>%date [%contextName] %level [%thread] [%tid] %logger{36} [%file : %line] %msg%n</Pattern>
            </layout>
        </encoder>
    </appender>

    <!-- 异步输出 -->
    <appender name="ASYNC" class="ch.qos.logback.classic.AsyncAppender">
        <!-- 不丢失日志.默认的,如果队列的80%已满,则会丢弃TRACT、DEBUG、INFO级别的日志 -->
        <discardingThreshold>0</discardingThreshold>
        <!-- 更改默认的队列的深度,该值会影响性能.默认值为256 -->
        <queueSize>512</queueSize>
        <!-- 添加附加的appender,最多只能添加一个 -->
        <appender-ref ref="HSP-ALL"/>
    </appender>

    <!--开发环境配置-->
    <springProfile name="dev">
        <logger name="com.allinfinance" level="${LOG_LEVEL}" additivity="false">
            <appender-ref ref="HSP-CONSOLE"/>
            <appender-ref ref="GRPC-LOG"/>
        </logger>

        <logger name="com.alipay" level="${LOG_LEVEL}" additivity="false">
            <appender-ref ref="HSP-CONSOLE"/>
            <appender-ref ref="GRPC-LOG"/>
        </logger>

        <root level="${LOG_LEVEL}">
            <appender-ref ref="HSP-CONSOLE"/>
            <appender-ref ref="GRPC-LOG"/>
        </root>
    </springProfile>

    <!--yc环境配置-->
    <springProfile name="yc">
        <logger name="com.allinfinance" level="${LOG_LEVEL}" additivity="false">
            <appender-ref ref="HSP-ALL"/>
            <appender-ref ref="GRPC-LOG"/>
        </logger>

        <logger name="com.alipay" level="${LOG_LEVEL}" additivity="false">
            <appender-ref ref="HSP-ALL"/>
            <appender-ref ref="GRPC-LOG"/>
        </logger>

        <root level="${LOG_LEVEL}">
            <appender-ref ref="HSP-ALL"/>
            <appender-ref ref="GRPC-LOG"/>
        </root>
    </springProfile>

    <!--sit环境配置-->
    <springProfile name="sit">
        <logger name="com.allinfinance" level="${LOG_LEVEL}" additivity="false">
            <appender-ref ref="HSP-ALL"/>
            <appender-ref ref="GRPC-LOG"/>
        </logger>

        <logger name="com.alipay" level="${LOG_LEVEL}" additivity="false">
            <appender-ref ref="HSP-ALL"/>
            <appender-ref ref="GRPC-LOG"/>
        </logger>

        <root level="${LOG_LEVEL}">
            <appender-ref ref="HSP-ALL"/>
            <appender-ref ref="GRPC-LOG"/>
        </root>
    </springProfile>

    <!--uat环境配置-->
    <springProfile name="uat">
        <logger name="com.allinfinance" level="${LOG_LEVEL}" additivity="false">
            <appender-ref ref="HSP-ALL"/>
            <appender-ref ref="GRPC-LOG"/>
        </logger>

        <logger name="com.alipay" level="${LOG_LEVEL}" additivity="false">
            <appender-ref ref="HSP-ALL"/>
            <appender-ref ref="GRPC-LOG"/>
        </logger>

        <root level="${LOG_LEVEL}">
            <appender-ref ref="HSP-ALL"/>
            <appender-ref ref="GRPC-LOG"/>
        </root>
    </springProfile>

    <!--生产环境配置-->
    <springProfile name="prod">
        <logger name="com.allinfinance" level="${LOG_LEVEL}" additivity="false">
            <appender-ref ref="HSP-ALL"/>
            <appender-ref ref="GRPC-LOG"/>
        </logger>

        <logger name="com.alipay" level="${LOG_LEVEL}" additivity="false">
            <appender-ref ref="HSP-ALL"/>
            <appender-ref ref="GRPC-LOG"/>
        </logger>

        <root level="${LOG_LEVEL}">
            <appender-ref ref="HSP-ALL"/>
            <appender-ref ref="GRPC-LOG"/>
        </root>
    </springProfile>
</configuration>
