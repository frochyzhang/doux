spring:
  application:
    name: dev-socket-server-tool

cn:
  lezoo:
    socket:
      server:
        bootstrap:
          enabled: true
        scaffold:
          server-metadata-list:
            - name: socket-test
              port: 8832
              handlerClassName: cn.lezoo.doux.socket.server.tool.handler.SocketServerHandler